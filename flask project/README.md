Need to download flask in order for most of the code to work, also start a virtual environment when doing so 
Weather.py will scrape the data from the website
Make sure to make get an openweathermap.org api key by making an account 
Also need a google maps api key to access the google maps api
Server.py will start the server using flask
Weather.html will essentially get the coords, city name, and the map thru the js and html5 apis used(no installations necessary)
requirements.txt will have all necessary things that need to be downloaded to make everything work

Change Logs:
 
Sept 4, 2024
Changed the Weather.html file to including handled form submission thru ajax since the host keeps refreshing on repeat
